<% if(message && message.length >0){ %>
  <div class="alert alert-warning alert-dismissible animated bounce" style="position: absolute;
	top: 10%;
	right: 35%;
	height: 100px;
	width: 400px;
	z-index: 99;">
    <button type="button" id="btn" class="close" data-dismiss="alert" aria-label="Close">
      <span aria-hidden="true">&times;</span>

    </button>
    <%= message %>
  </div>
  <% } %>
    <% layout("layout") -%>
    <div id="particles-js"></div>
      <div class="col-sm-3" style=" padding: 10px; margin-top: 50px; " overflow-x: hidden;>
        <div class="col-lg-12">
          <% include ../partials/adminpanel %>
        </div>
      </div>
      <div class="container">
      <div class="col-sm-6">

        <div class="container">
          <div class="panel panel-info  col-sm-9 page-content" id="myScrollspy" style="padding: 0px; margin-top: 80px; min-height:500px; ">
            <div class="panel-heading" style="color: #ffffff;">

              <a href="/manageaccount">Manage Account</a> >> User Account Details

            </div>
            <div style="padding: 3%;">
              <h1>
                <span>
                  <%= user.profile.lastName %>,
                    <%= user.profile.firstName %>
                      <%= user.profile.middleName %>
                </span>
                <span style=" font-size: 20px; align:right ">
                  <%= user.idNumber %>
                </span>
              </h1>
              </br>
              <h3>
                <span>Account Type:
                  <%= user.user %>
                </span>
              </h3>
              <h3>
                <span>Year and Section:
                  <%= user.yrLvl %> -
                    <%= user.section %>
                </span>
              </h3>
              <h3>
                <span>Gender:
                  <%= user.gender %>
                </span>
              </h3>
              <h3>
                <span>Age:
                  <%= user.age %>
                </span>
              </h3>
              <h3>
                <span>Birthday:
                  <%= moment(user.birthdate).format("LL") %>
                </span>
              </h3>

              <h3>
                <span>Address:
                  <%= user.address %>
                </span>
              </h3>

              <h3>
                <span>Contact:
                  <%= user.contact %>
                </span>
              </h3>
              <h3>
                <span>
                  <form action="/manageaccount/<%= user._id %>" method="POST">
                    <button class="btn btn-info" style="float: right; padding: 2%; margin: 2%;">Disable Account</button>
                  </form>
                </span>
              </h3>
            </div>
          </div>
        </div>
      </div>
      </div>
      </div>